import {connect} from "react-redux";
import MainDoc from "./MainDoc";

let mapStateToProps = () => {
  return null;
};

let mapDispatchToProps = () => {
    return null;
};

const MainDocContainer = connect(mapStateToProps, mapDispatchToProps)(MainDoc);

export default MainDocContainer;