import {connect} from "react-redux";
import ShowMainDoc from "./ShowMainDoc";

let mapStateToProps = () => {
  return null;
};

let mapDispatchToProps = () => {
    return null;
};

const ShowMainDocContainer = connect(mapStateToProps, mapDispatchToProps)(ShowMainDoc);

export default ShowMainDocContainer;